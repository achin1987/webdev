<% include partials/header %>

<h1>This is the camp ground page!</h1>

<% campgrounds.forEach((campground) =>{ %>
    <div>
        <h4><%= campground.name%></h4>
        <img src="<%= campground.image %>">
    </div>

<% }); %>

<% include partials/footer %>
